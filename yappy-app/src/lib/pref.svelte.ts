// Preferencias VIVAS compartidas entre la cinta y la trastienda (el cajón
// vive superpuesto: el cambio debe verse al instante, sin recargar).
export const pref = $state({
  dosColumnas: false,
});
